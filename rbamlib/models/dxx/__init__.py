"""
The `dxx` provides diffusion coefficient scaling and models

Additional models and scaling functions are grouped into packages that correspond to specific waves, like `chorus` or `hiss`.

Packages:
    - `chorus` : Models of chorus waves
    - `hiss` : Models of hiss waves
    - `emic` : Models of EMIC waves
    - `lightning` : Models of lightning generated whistler waves
    - `vlf` : Models of anthropogenic VLF transmitters
"""